---
route:
    - id: demo

    - from: direct:start

    - transform:
    - method:
        bean: myBean
        method: saySomething

    - filter:
    - simple: ${body} contains 'foo'

    - filter:
    - simple: ${headers} contains 'bar'

    - choice:
    - when:
    - jsonpath: $.store.book[?(@.price > 10)]
    - to: direct:a
    - otherwise:
    - to: direct:b
---
route:
    - id: demo2
    - from: timer:test
    - to: log